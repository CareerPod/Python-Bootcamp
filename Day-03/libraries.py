# A module is the building block: a single file containing code.
# A library is a collection of related modules bundled together to provide extensive functionality.

import numpy as np

# Using a library (NumPy) with its modules and functions
array = np.array([1, 2, 3])
print(array)
